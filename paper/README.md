# IEEE LaTeX Report

This directory contains an IEEEtran-style LaTeX paper for the stablecoin peg-stability research.

Build from this directory:

```bash
latexmk -pdf stablecoin_ieee_report.tex
```

The report imports figures from `../charts/`, so keep the repository layout unchanged when compiling.
